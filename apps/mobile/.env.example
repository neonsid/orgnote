# Same Convex deployment as the web app (Convex Dashboard → Settings → Deployment URL)
EXPO_PUBLIC_CONVEX_URL=

# Clerk Dashboard → API Keys → Publishable key (Expo / native)
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
